Git: https://github.com/KiranMangu/task-manager-frontend
Dockerfile: Dockerfile

Url: http://localhost:4200
port: 4200

Note:
**** wrapper.sh should be saved as unix vile (i.e., LF is getting replaced with CRLF) and is failing while building the docker. Windows is the develoopment environment and the .sh file is saving as windows file. As a remedy save the file as Unix file and use it  **** 
Uses Material UI Framework
Uses ReactiveForms